'use strict';

/**
 * premium-pipe-about controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::premium-pipe-about.premium-pipe-about');
