'use strict';

/**
 * premium-pipe-about service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::premium-pipe-about.premium-pipe-about');
