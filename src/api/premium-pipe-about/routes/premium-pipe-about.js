'use strict';

/**
 * premium-pipe-about router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::premium-pipe-about.premium-pipe-about');
