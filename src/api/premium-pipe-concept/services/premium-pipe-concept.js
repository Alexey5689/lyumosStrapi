'use strict';

/**
 * premium-pipe-concept service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::premium-pipe-concept.premium-pipe-concept');
