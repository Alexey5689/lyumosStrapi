'use strict';

/**
 * premium-pipe-concept router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::premium-pipe-concept.premium-pipe-concept');
